{
  "name": "adurun-character-sheet",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "client",
    "shared"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build:client",
    "postbuild": "rm -rf dist && cp -R client/dist dist",
    "build:client": "npm run build --workspace client",
    "dev:client": "npm run dev --workspace client",
    "preview:client": "npm run preview --workspace client",
    "lint": "echo \"add linter\"",
    "test": "echo \"add tests\""
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3",
    "wrangler": "^4.56.0",
    "wscat": "^6.1.0"
  }
}
